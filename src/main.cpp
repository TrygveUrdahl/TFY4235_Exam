#include <iostream>

int main(int argc, char** argv) {
  if (argc < 2) {
    // std::cout << "Requires argument(s): " << std::endl;
    // std::cout << "\tint n: problemsize" << std::endl;
    // throw std::logic_error("Wrong program arguments! ");
  }
  return 0;
}
